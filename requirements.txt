# COMP0248 CW1 — runtime dependencies
# Install with:  python -m pip install -r requirements.txt

torch>=2.0.0
numpy>=1.24.0
opencv-python>=4.8.0
matplotlib>=3.7.0

# Notes
# -----
# torchvision is not listed because nothing in src/ imports it. It is harmless
# if present (it ships alongside CUDA PyTorch), but no pretrained backbone, no
# torchvision.models.detection component, and no high-level detection or
# segmentation framework is used anywhere in the submitted model, training
# loop or evaluation code.
#
# What is used from PyTorch, and what is written here:
#   built-in, used as provided   nn.Conv2d, nn.BatchNorm2d, nn.ConvTranspose2d,
#                                nn.BCEWithLogitsLoss, nn.CrossEntropyLoss,
#                                torch.optim.AdamW, CosineAnnealingLR
#   written for this submission  the architecture (nn.Module subclasses), the
#                                Dataset and its splitting and augmentation,
#                                the training loop, the loss composition, and
#                                Dice, IoU, GIoU, the confusion matrix and
#                                macro-F1
#
# The annotation pipeline (SAM3, Label Studio) is a separate,
# offline step that produces ground-truth masks only. It is never invoked
# during training or inference, so its dependencies are not listed here.
